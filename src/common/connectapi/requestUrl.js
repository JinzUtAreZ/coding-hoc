const userRequests = {
  fetchAllUsers: `/users/`,
  fetchAllProducts: "/products/",
};

const productRequests = {
  fetchAllUsers: `/users/`,
  fetchAllProducts: "/products/",
};

export { userRequests, productRequests };
